# 开发规范（一页版）

每条 1–3 行，只写硬规则；解释与示例放链接的权威文档，此处禁止复述。

## 硬规则

1. **CI 是唯一权威**：lint、类型检查、测试全在 CI 强制；master 分支保护，禁止直推。
2. **默认 `strict`**：类型严格模式全开；禁止新增 `as unknown` / `# type: ignore` 逃逸（存量进基线冻结，见 `scripts/verify-rules.md`）。
3. **判别联合收口**：switch 只基于判别标签，闭合联合以穷尽检查（`assertNever`）结尾。
4. **跨模块标识符必须是命名类型**，禁止裸字符串（branded type / newtype / 类）。
5. **扩展点优于改核心**：新行为挂接口、事件、钩子；改核心模块必须同 PR 更新架构文档。
6. **配置显式解析**：默认值只有一个 `resolve()` 步骤；禁止散落的隐式默认。
7. **异常不静默**：空 catch 必须命名错误并说明为何忽略；一个 try 只包一条语句。
8. **文档只写当前事实**：禁止"已实现 / 曾经 / 计划中"状态词；一事实一归属，其余放链接。
9. **决策留痕**：持久架构决策与实现同 PR 写入 `.agents/notes/`（格式与生命周期见 `.agents/notes/README.md`）。
10. **测试验证世界**：断言走真实入口、外部重验；只 mock 贵/不确定的边界；对外可见输出固化为快照。

## 权威文档

- 架构地图：`docs/architecture.md`
- 测试政策：`docs/testing.md`
- 机械规则清单：`docs/verify-rules.md`
- 决策笔记：`.agents/notes/README.md`

> 字数预算：本文件 ≤ 800 词。超限先归位到权威文档，再压缩，最后才讨论提额度。
